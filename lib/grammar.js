/**
 *  Default map of tag names to grammar macro functions.
 *
 *  @property grammar
 */
module.exports = {
  include: require('./include'),
  source: require('./source'),
  exec: require('./exec')
}
